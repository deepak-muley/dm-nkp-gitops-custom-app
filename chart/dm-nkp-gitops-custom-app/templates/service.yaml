apiVersion: v1
kind: Service
metadata:
  name: {{ include "dm-nkp-gitops-custom-app.fullname" . }}
  labels:
    {{- include "dm-nkp-gitops-custom-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "dm-nkp-gitops-custom-app.selectorLabels" . | nindent 4 }}
